const status = {
  CONSUMED: 'CONSUMED',
  EXPIRED: 'EXPIRED',
  ACTIVE: 'ACTIVE',
  INACTIVE: 'INACTIVE'
};

const userRole = {
  USER: 'USER',
  ADMIN: 'ADMIN'
};

module.exports = { status, userRole };
