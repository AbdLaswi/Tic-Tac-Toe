const gamePlayerService = require('./game-player');
const gameService = require('./game');
const userService = require('./user');
const authService = require('./auth');

module.exports = {
  gamePlayerService,
  gameService,
  userService,
  authService
};
